import { describe, expect, it, vi } from 'vitest'; import { NextRequest } from 'next/server'; import { POST } from '@/app/api/fusion/launch/route';
const request=()=>new NextRequest('http://openmaic.local/api/fusion/launch',{method:'POST',body:JSON.stringify({classroomLaunchCode:'code'})});
describe('F16 launch route', () => {
 it('never returns delegation token', async () => { vi.stubEnv('DEEPTUTOR_FUSION_BASE_URL','http://dt.local'); vi.stubGlobal('fetch',vi.fn(async (_url,init)=>{const sessionId=JSON.parse(String(init?.body)).lessonSessionId; return new Response(JSON.stringify({token:'secret',tokenId:'t',learnerId:'learner-1',audience:'openmaic',scope:['profile:read','diagnosis:request','classroom-event:write','profile-update:submit'],expiresAt:9999999999,lessonSessionId:sessionId}),{status:200});})); const body=await (await POST(request())).json(); expect(body.learnerId).toBe('learner-1'); expect(JSON.stringify(body)).not.toContain('secret'); });
 it('rejects invalid audience, scope, expiry or session binding', async () => { vi.stubEnv('DEEPTUTOR_FUSION_BASE_URL','http://dt.local'); vi.stubGlobal('fetch',vi.fn(async()=>new Response(JSON.stringify({token:'secret',tokenId:'t',learnerId:'learner-1',audience:'wrong',scope:[],expiresAt:0,lessonSessionId:'wrong'}),{status:200}))); expect((await POST(request())).status).toBe(401); });
});
