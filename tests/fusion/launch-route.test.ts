import { describe, expect, it, vi } from 'vitest'; import { NextRequest } from 'next/server'; import { POST } from '@/app/api/fusion/launch/route';
describe('F16 launch route', () => it('never returns delegation token', async () => { vi.stubEnv('DEEPTUTOR_FUSION_BASE_URL','http://dt.local'); vi.stubGlobal('fetch', vi.fn(async () => new Response(JSON.stringify({token:'secret',tokenId:'t',learnerId:'learner-1',audience:'openmaic',scope:['profile:read'],expiresAt:9999999999,lessonSessionId:'x'}),{status:200}))); const body=await (await POST(new NextRequest('http://openmaic.local/api/fusion/launch',{method:'POST',body:JSON.stringify({classroomLaunchCode:'code'})}))).json(); expect(body.learnerId).toBe('learner-1'); expect(JSON.stringify(body)).not.toContain('secret'); }));
