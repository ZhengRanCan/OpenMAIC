import { describe, expect, it } from 'vitest'; import { FusionOutboxStore } from '@/lib/fusion/outbox/store'; import { processOne } from '@/lib/fusion/outbox/worker'; import type { ProfileUpdateCandidate } from '@/lib/fusion/profile-update-candidate';
describe('F14 worker', () => it('records retryable failures without losing the candidate', async () => { const store = new FusionOutboxStore(); const candidate: ProfileUpdateCandidate = { schemaVersion: 'v1', candidateId: 'c', idempotencyKey: 'k', lessonSessionId: 'l', sourceEventIds: [], mappingId: 'm', mappingRevision: '1', observations: [{ kind: 'assessment_result', lessonKnowledgePointId: 'kp', value: 'x' }], createdAt: 'x' }; store.enqueue(candidate); await processOne(store, { submit: async () => { throw new Error('offline'); } }); expect(store.get('k')?.status).toBe('retry_scheduled'); }));
