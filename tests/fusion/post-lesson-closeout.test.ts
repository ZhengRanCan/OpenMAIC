import { describe, expect, it } from 'vitest';
import { FusionOutboxStore } from '@/lib/fusion/outbox/store'; import { completeLesson, summarizeDelivery } from '@/lib/fusion/post-lesson-closeout'; import type { ClassroomObservationLedger } from '@/lib/fusion/classroom-observation-ledger';
const ledger: ClassroomObservationLedger = { schemaVersion:'v1', lessonSessionId:'l', courseId:'c', createdAt:'2026-01-01T00:00:00Z', revision:1, observations:[{ observationId:'o',sourceEventId:'e',correlationId:'c',checkpointId:'p',sceneId:'s',lessonKnowledgePointIds:['lesson-linear-function-slope'],originalQuestion:'q',studentAnswer:'a',localAssessment:{gradingMode:'local'},diagnosisSummary:{correctness:'incorrect'},occurredAt:'2026-01-01T00:00:00Z',recordedAt:'2026-01-01T00:00:00Z'}] };
describe('F15 post lesson closeout', () => it('queues without blocking completion and never claims long-term profile update', () => { const outbox=new FusionOutboxStore(); const summary=completeLesson(ledger,outbox); expect(summary).toMatchObject({immediateConclusion:'classroom_completed',deliveryStatus:'queued',longTermProfileStatus:'not_confirmed'}); expect(completeLesson(ledger,outbox).deliveryStatus).toBe('duplicate'); expect(summarizeDelivery('dead_letter','submission_rejected')).toMatchObject({deliveryStatus:'dead_letter',reasonCode:'submission_rejected',longTermProfileStatus:'not_confirmed'}); }));
